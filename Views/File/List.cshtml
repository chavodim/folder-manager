@model FileListModel

<h3>
    @Model.Folder.FolderName
</h3>

<div class="row">

    @if(Model.Folder.Files != null){
            <table class="table table-hover">                
                    @foreach (var file in Model.Folder.Files){
                <tbody> 
                    <tr>
                        <td>
                            @file.CustomFileName
                        </td>
                        <td>
                            @file.CustomFileFormat
                        </td>
                        <td>
                            <a class="btn btn-info" type="button" href="@file.CustomFilePath" 
                            download="@file.CustomFileName">
                                Download
                            </a>
                        </td>
                    </tr>
                </tbody>
                }
            </table>
    }

</div>